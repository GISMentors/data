# GISMentors training data

## Poznámky

```
sudo apt install git-lfs
```

Inicializace:

```
git lfs install
git lfs track "*.zip"
git add .gitattributes
git add *.zip
git commit -m "Add data"
git push origin main
git lfs ls-files
```
